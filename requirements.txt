# Core training stack
torch>=2.4.0
transformers>=4.46.0
peft>=0.13.0
bitsandbytes>=0.44.0
accelerate>=1.0.0

# Data + utils
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.66.0

# Proposer agent (LLM-driven config generation)
anthropic>=0.40.0
